# 与 TRAINING_PRESETS["step5"] 一致；主路径：mainline_composite 选模 + 长度 36 对齐
1:
  train_batch_size: 1024
  train_label_max_length: 36
  train_dynamic_padding: true
  epochs: 30
  full_bleu_eval:
    mode: interval
    every_epochs: 3
  full_bleu_decode_strategy: greedy
  loss_weight_repeat_ul: 0.06
  loss_weight_terminal_clean: 0.04
  terminal_clean_span: 3
  min_lr_ratio: 0.1
  adv: 0.005
  lr: 5.0e-4
  coef: 1
2:
  train_batch_size: 512
  train_label_max_length: 36
  train_dynamic_padding: true
  epochs: 30
  full_bleu_eval:
    mode: interval
    every_epochs: 3
  full_bleu_decode_strategy: greedy
  loss_weight_repeat_ul: 0.06
  loss_weight_terminal_clean: 0.04
  terminal_clean_span: 3
  min_lr_ratio: 0.1
  adv: 0.005
  lr: 1.0e-3
  coef: 0.1
3:
  train_batch_size: 1024
  train_label_max_length: 36
  train_dynamic_padding: true
  epochs: 30
  full_bleu_eval:
    mode: interval
    every_epochs: 3
  full_bleu_decode_strategy: greedy
  loss_weight_repeat_ul: 0.06
  loss_weight_terminal_clean: 0.04
  terminal_clean_span: 3
  min_lr_ratio: 0.1
  adv: 0.005
  lr: 5.0e-4
  coef: 0.5
4:
  train_batch_size: 1024
  train_label_max_length: 36
  train_dynamic_padding: true
  epochs: 50
  full_bleu_eval:
    mode: interval
    every_epochs: 3
  full_bleu_decode_strategy: greedy
  loss_weight_repeat_ul: 0.06
  loss_weight_terminal_clean: 0.04
  terminal_clean_span: 3
  min_lr_ratio: 0.1
  adv: 0.005
  lr: 1.0e-3
  coef: 0.5
5:
  train_batch_size: 1024
  train_label_max_length: 36
  train_dynamic_padding: true
  epochs: 30
  full_bleu_eval:
    mode: interval
    every_epochs: 3
  full_bleu_decode_strategy: greedy
  loss_weight_repeat_ul: 0.06
  loss_weight_terminal_clean: 0.04
  terminal_clean_span: 3
  min_lr_ratio: 0.1
  adv: 0.005
  lr: 1.0e-3
  coef: 0.5
6:
  train_batch_size: 1024
  train_label_max_length: 36
  train_dynamic_padding: true
  epochs: 30
  full_bleu_eval:
    mode: interval
    every_epochs: 3
  full_bleu_decode_strategy: greedy
  loss_weight_repeat_ul: 0.06
  loss_weight_terminal_clean: 0.04
  terminal_clean_span: 3
  min_lr_ratio: 0.1
  adv: 0.005
  lr: 1.0e-3
  coef: 0.5
7:
  train_batch_size: 1024
  train_label_max_length: 36
  train_dynamic_padding: true
  epochs: 30
  full_bleu_eval:
    mode: interval
    every_epochs: 3
  full_bleu_decode_strategy: greedy
  loss_weight_repeat_ul: 0.06
  loss_weight_terminal_clean: 0.04
  terminal_clean_span: 3
  min_lr_ratio: 0.1
  adv: 0.005
  lr: 1.0e-4
  coef: 0.5
8:
  train_batch_size: 1024
  train_label_max_length: 36
  train_dynamic_padding: true
  epochs: 30
  full_bleu_eval:
    mode: interval
    every_epochs: 3
  full_bleu_decode_strategy: greedy
  loss_weight_repeat_ul: 0.06
  loss_weight_terminal_clean: 0.04
  terminal_clean_span: 3
  min_lr_ratio: 0.1
  adv: 0.005
  lr: 5.0e-4
  coef: 1
