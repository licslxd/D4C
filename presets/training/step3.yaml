# 与 TRAINING_PRESETS["step3"] 一致（文件名 = 预设名）；Step 3 使用 `python code/d4c.py step3 --preset step3`
1:
  train_batch_size: 1024
  epochs: 30
  full_bleu_eval:
    mode: interval
    every_epochs: 2
  min_lr_ratio: 0.1
  adv: 0.005
  lr: 5.0e-4
  coef: 1
2:
  train_batch_size: 1024
  epochs: 30
  full_bleu_eval:
    mode: interval
    every_epochs: 2
  min_lr_ratio: 0.1
  adv: 0.005
  lr: 1.0e-3
  coef: 0.1
3:
  train_batch_size: 1024
  epochs: 30
  full_bleu_eval:
    mode: interval
    every_epochs: 2
  min_lr_ratio: 0.1
  adv: 0.005
  lr: 5.0e-4
  coef: 0.5
4:
  train_batch_size: 1024
  epochs: 30
  full_bleu_eval:
    mode: interval
    every_epochs: 2
  min_lr_ratio: 0.1
  adv: 0.005
  lr: 1.0e-3
  coef: 0.5
5:
  train_batch_size: 1024
  epochs: 30
  full_bleu_eval:
    mode: interval
    every_epochs: 2
  min_lr_ratio: 0.1
  adv: 0.005
  lr: 1.0e-3
  coef: 0.5
6:
  train_batch_size: 1024
  epochs: 30
  full_bleu_eval:
    mode: interval
    every_epochs: 2
  min_lr_ratio: 0.1
  adv: 0.005
  lr: 1.0e-3
  coef: 0.5
7:
  train_batch_size: 1024
  epochs: 30
  full_bleu_eval:
    mode: interval
    every_epochs: 2
  min_lr_ratio: 0.1
  adv: 0.005
  lr: 1.0e-4
  coef: 0.5
8:
  train_batch_size: 1024
  epochs: 30
  full_bleu_eval:
    mode: interval
    every_epochs: 2
  min_lr_ratio: 0.1
  adv: 0.005
  lr: 5.0e-4
  coef: 1
